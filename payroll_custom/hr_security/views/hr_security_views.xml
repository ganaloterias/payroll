<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Menú de Empleados -->
    <record id="hr.menu_hr_root" model="ir.ui.menu">
        <field name="groups_id" eval="[(4, ref('group_hr_base'))]"/>
    </record>

    <!-- Menú de Ausencias -->
    <record id="hr_holidays.menu_hr_holidays_root" model="ir.ui.menu">
        <field name="groups_id" eval="[(4, ref('group_hr_base'))]"/>
    </record>
</odoo> 